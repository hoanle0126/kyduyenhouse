import electronicsImg from "resources/assets/electronics.png";

const Electronics = ({ className }) => {
    return <img src={electronicsImg} alt="" className={className} />;
};

export default Electronics;
